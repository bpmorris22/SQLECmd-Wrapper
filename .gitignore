# Eric Zimmerman's SQLECmd binaries + maps — downloaded at runtime, NOT redistributed here
SQLECmd.exe
SQLECmd.dll
SQLECmd.runtimeconfig.json
SQLite.Interop.dll
Maps/

# Forensic output / case data — never commit
Exported CSVs/
*.csv
*.db
*.sqlite
*.sqlite-*

# per-machine settings sidecar (column widths etc.)
SQLECmd-Wrapper.settings.json

# scratch
*.tmp
*.log
